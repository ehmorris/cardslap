<ul class="cards" data-deck_id="<%= @deck.id %>">
  <%= render :partial => 'cards/new-card-form', :locals => { :deck => @deck, :card => @card } %>
  <% @cards.each do |card| %>
    <% if card.bin == 'main' %>
      <%= render :partial => 'cards/card', :locals => { :deck => @deck, :card => card } %>
    <% end %>
  <% end %>
  <li class="reorder-target last"></li>
  <li class="card-button"><%= link_to 'Enter quiz mode', deck_quiz_index_path(@deck), :class => "enter-quiz" %></li>
</ul>

<div id="bin">
  <h2>Memorized Pile</h2>
  <% @cards.each do |card| %>
    <% if card.bin == 'bin' %>
      <%= render :partial => 'cards/card', :locals => { :deck => @deck, :card => card } %>
    <% end %>
  <% end %>
</div>

<div class="exportshare">
  <a class="export" href="<%= request.url %>.csv"><i class="icon-download-alt"></i></a>
  <a class="share" href="#" data-toggle="dropdown"><i class="icon-share-alt"></i> share</a>
  <%= render :partial => 'shares/shares', :locals => { :deck => @deck, :share => @share } %>
</div>
