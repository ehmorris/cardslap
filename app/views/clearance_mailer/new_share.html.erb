You've been cardslapped!

<%= @user.email %> has shared their deck, "<%= @deck.name %>" with you on Cardslap.

<%= link_to 'Access it here', deck_share_path(@deck.id, @share.id, :anchor => @invitee) %>
